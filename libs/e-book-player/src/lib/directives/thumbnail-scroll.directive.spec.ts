import { ThumbnailScrollDirective } from './thumbnail-scroll.directive';

describe('ThumbnailScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbnailScrollDirective();
    expect(directive).toBeTruthy();
  });
});
